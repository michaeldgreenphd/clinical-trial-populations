# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Logs
*.log
extraction.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Testing
.pytest_cache/
.coverage
htmlcov/

# FDA summary PDFs — full corpus is large (1,400+ files) and lives outside git.
# The pilot subset in Data/pilot_summary_statements/ IS tracked so GitHub Actions
# can run the pilot extraction without pulling the full archive.
data/fda_pdfs/
Data/FDA Summary Statements/
!Data/pilot_summary_statements/
!Data/pilot_summary_statements/**

# Pilot target list of the 50 largest trials — generated by
# scripts/generate_pilot_targets.py and consumed by an external
# (Google Colab) literature-search pilot. Tracked in git so the
# downstream pipeline has a stable input.
!data/pilot_clinical_trials_targets.csv

# Peer-reviewed AI/ML manuscript PDFs — same pattern: the full archive is
# ignored; the small pilot set used by scripts/extraction/extract_paper_ses.py
# IS tracked so GitHub Actions can run the literature pilot. Both casings of
# the archive directory are covered defensively.
Data/AIML Manuscripts/
data/AIML Manuscripts/
!Data/pilot_AIML_manuscripts/
!Data/pilot_AIML_manuscripts/**
!data/pilot_AIML_manuscripts/
!data/pilot_AIML_manuscripts/**
